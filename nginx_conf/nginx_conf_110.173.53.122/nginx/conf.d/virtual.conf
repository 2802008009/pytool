#
# A virtual host using mix of IP-, name-, and port-based configuration
# pay.2500ds.com szgly2.com不开启https

#server {
#    listen       8000;
#    listen       somename:8080;
#    server_name  somename  alias  another.alias;

#    location / {
#        root   html;
#        index  index.html index.htm;
#    }
#}
upstream mysrvs {
    ip_hash;
    server 202.55.16.114:80;
    server 202.55.16.104:80;
}
server {
    listen 80;
    server_name d.2500ds.com;
    location / {
        root /home/www;
        index index.html index.php;
    }
}
server
{
    listen 80;
    server_name admin.57by.com admin.2500ds.com admin.dsyl88.com pay.2500ds.com szsly2.com *.szsly2.com szgly1.com *.szgly1.com pay.ds876.com;
    large_client_header_buffers 4 16k;
    client_max_body_size 300m;
    client_body_buffer_size 256k;
    location / {
        proxy_pass http://202.55.16.114;
        proxy_redirect off;
        proxy_hide_header X-Powered-By;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header REMOTE-HOST $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
    location ~ .*\.(ico|jpg|jpeg|bmp|gif|png|js|css|swf)$ {
        proxy_cache cache_one;
        proxy_cache_valid 200 304 12h;
        proxy_cache_key $host$uri$is_args$args;
        proxy_set_header Host $host;
        expires 1h;
        proxy_pass http://202.55.16.114;
    }
}
server {
    listen 80;
    server_name dsyl88.com www.dsyl88.com data.dsyl88.com;

    location ^~ /.well-known/acme-challenge/ {
        alias /home/www/challenges/;
        try_files $uri =404;
    }

    location / {
        rewrite ^/(.*)$ https://www.ds3721.com/$1 permanent;
    }
}
server {
    listen 80;
    server_name m.dsyl88.com m.ds876.com wap.130hs.com wap.jyzd8.com wap.cc0839.com;

    location ^~ /.well-known/acme-challenge/ {
        alias /home/www/challenges/;
        try_files $uri =404;
    }

    location / {
        rewrite ^/(.*)$ https://m.dsyl88.com/$1 permanent;
    }
}
server {
    listen 80;
    server_name m.130911.com wap.130911.com m.3366ds.com wap.3366ds.com m.2500ds.com wap.2500ds.com m.57by.com wap.57by.com;
    location / {
        rewrite ^/(.*)$ https://m.dsyl88.com/$1 permanent;#HKLINE ->dsyl88.com
    }
}
server {
    listen 80;
    server_name 130911.com *.130911.com 3366ds.com *.3366ds.com 2500ds.com *.2500ds.com 57by.com *.57by.com ds876.com *.ds876.com 130hs.com *.130hs.com jyzd8.com *.jyzd8.com cc0839.com *.cc0839.com;
    location / {
        rewrite ^/(.*)$ https://www.dsyl88.com/$1 permanent;#HKLINE ->dsyl88.com
    }
}
server {
    listen               443 ssl;
    server_name          www.dsyl88.com dsyl88.com m.dsyl88.com;
    server_tokens        off;
    # 中间证书 + 站点证书
    ssl_certificate      /home/ssl/chained.pem;
    # 创建 CSR 文件时用的密钥
    ssl_certificate_key  /home/ssl/domain.key;
    # https://github.com/cloudflare/sslconfig/blob/master/conf
    ssl_ciphers                EECDH+CHACHA20:EECDH+CHACHA20-draft:EECDH+AES128:RSA+AES128:EECDH+AES256:RSA+AES256:EECDH+3DES:RSA+3DES:!MD5;

    # 如果启用了 RSA + ECDSA 双证书，Cipher Suite 可以参考以下配置：
    # ssl_ciphers              EECDH+CHACHA20:EECDH+CHACHA20-draft:EECDH+ECDSA+AES128:EECDH+aRSA+AES128:RSA+AES128:EECDH+ECDSA+AES256:EECDH+aRSA+AES256:RSA+AES256:EECDH+ECDSA+3DES:EECDH+aRSA+3DES:RSA+3DES:!MD5;

    ssl_prefer_server_ciphers  on;

    ssl_protocols              TLSv1 TLSv1.1 TLSv1.2;

    ssl_session_cache          shared:SSL:50m;
    ssl_session_timeout        1d;
    ssl_session_tickets        on;

    ssl_stapling               on;
    ssl_stapling_verify        on;

    # 根证书 + 中间证书
    # https://imququ.com/post/why-can-not-turn-on-ocsp-stapling.html
    ssl_trusted_certificate    /home/ssl/full_chained.pem;

    resolver                   8.8.8.8 valid=300s;
    resolver_timeout           10s;

    access_log                 off;
    large_client_header_buffers 4 16k;
    client_max_body_size 300m;
    client_body_buffer_size 256k;

    if ($request_method !~ ^(GET|HEAD|POST|OPTIONS)$ ) {
        return           444;
    }

    location ~ .*\.(ico|jpg|jpeg|bmp|gif|png|js|css|swf)$ {
        proxy_cache cache_one;
        proxy_cache_valid 200 304 12h;
        proxy_cache_key $host$uri$is_args$args;
        proxy_set_header Host $host;
        expires 1h;
        proxy_pass http://mysrvs;
    }
    location / {
        proxy_pass http://mysrvs;
        proxy_redirect off;
        proxy_hide_header X-Powered-By;
        proxy_set_header Via ssl.ds;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header REMOTE-HOST $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
server {
    listen               443 ssl;
    server_name          data.dsyl88.com;
    server_tokens        off;
    # 中间证书 + 站点证书
    ssl_certificate      /home/ssl/chained.pem;
    # 创建 CSR 文件时用的密钥
    ssl_certificate_key  /home/ssl/domain.key;
    # https://github.com/cloudflare/sslconfig/blob/master/conf
    ssl_ciphers                EECDH+CHACHA20:EECDH+CHACHA20-draft:EECDH+AES128:RSA+AES128:EECDH+AES256:RSA+AES256:EECDH+3DES:RSA+3DES:!MD5;

    # 如果启用了 RSA + ECDSA 双证书，Cipher Suite 可以参考以下配置：
    # ssl_ciphers              EECDH+CHACHA20:EECDH+CHACHA20-draft:EECDH+ECDSA+AES128:EECDH+aRSA+AES128:RSA+AES128:EECDH+ECDSA+AES256:EECDH+aRSA+AES256:RSA+AES256:EECDH+ECDSA+3DES:EECDH+aRSA+3DES:RSA+3DES:!MD5;

    ssl_prefer_server_ciphers  on;

    ssl_protocols              TLSv1 TLSv1.1 TLSv1.2;

    ssl_session_cache          shared:SSL:50m;
    ssl_session_timeout        1d;
    ssl_session_tickets        on;

    ssl_stapling               on;
    ssl_stapling_verify        on;

    # 根证书 + 中间证书
    # https://imququ.com/post/why-can-not-turn-on-ocsp-stapling.html
    ssl_trusted_certificate    /home/ssl/full_chained.pem;

    resolver                   8.8.8.8 valid=300s;
    resolver_timeout           10s;

    access_log                 off;
    large_client_header_buffers 4 16k;
    client_max_body_size 300m;
    client_body_buffer_size 256k;

    if ($request_method !~ ^(GET|HEAD|POST|OPTIONS)$ ) {
        return           444;
    }

    location ~ .*\.(ico|jpg|jpeg|bmp|gif|png|js|css|swf)$ {
        proxy_cache cache_one;
        proxy_cache_valid 200 304 12h;
        proxy_cache_key $host$uri$is_args$args;
        proxy_set_header Host $host;
        expires 1h;
        proxy_pass http://202.55.16.122;
    }
    location / {
        proxy_pass http://202.55.16.122;
        proxy_redirect off;
        proxy_hide_header X-Powered-By;
        proxy_set_header Via ssl.ds;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header REMOTE-HOST $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
